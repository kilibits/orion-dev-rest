# orion-dev-rest
Rest API serving data from local sqlite database

Place sqlite file at the root of the directory before running main file - `go run api.go`
Sqlite DB for this application - https://github.com/uint16/makler/tree/master/database